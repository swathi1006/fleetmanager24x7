source 'https://github.com/CocoaPods/Specs.git'
platform :ios, '14.0'

target 'Runner' do
  use_frameworks!

  # Add Flutter pods and dependencies
  pod 'GoogleMaps', '~> 7.1.0'

  # flutter_image_compress is handled by the Flutter plugin mechanism
  # It will automatically add the right pod dependencies if you run `flutter pub get`

  target 'RunnerTests' do
    inherit! :search_paths
  end
end
